node_modules/
dist/
.wrangler/
*.local
.commandcode/
__pycache__/

# Pomodorus proxy worker (private, user-deployed) — never commit
proxy-worker/

# Server (local dev secrets + build output)
server/.env
server/dist/
bun.lock
bun.lockb

# Dashboard-managed mail relay (see email-worker/worker.js, deployed via paste)
worker/
