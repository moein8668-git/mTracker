{
  // Cloudflare Workers — assets-only deploy (no server code).
  // Deploy: npx wrangler deploy   (after M1/Vite this directory becomes ./dist)
  "name": "mtracker",
  "compatibility_date": "2026-08-01",
  "assets": {
    "directory": "./public"
  }
}
